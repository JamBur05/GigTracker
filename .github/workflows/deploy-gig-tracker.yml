name: "Deploy Gig Tracker"

on:
  push:
    tags:
      - "gig-tracker/v*"

jobs:
  deploy:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Setup .NET Core
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: '6.0.x'

      - name: Restore dependencies
        run: dotnet restore JamBur05/GigTracker/GigTracker.csproj

      - name: Build the project
        run: dotnet build JamBur05/GigTracker/GigTracker.csproj -c Release --no-restore

      - name: Publish the project
        run: dotnet publish JamBur05/GigTracker/GigTracker.csproj -c Release --self-contained -r win-x64 -p:PublishSingleFile=true --no-build
